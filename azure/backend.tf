terraform {
  #  backend "azurerm" {
  #    resource_group_name  = "<tfstate-resource-group>"      # e.g. "tfstate-rg"
  #    storage_account_name = "<tfstate-storage-account>"     # must be globally unique
  #    container_name       = "<tfstate-container>"           # e.g. "tfstate"
  #    key                  = "fintrade.azure.terraform.tfstate"
  #  }
}
